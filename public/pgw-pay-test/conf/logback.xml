<?xml version="1.0" encoding="UTF-8"?>
<configuration>

	<contextName>pgw-pay-test</contextName>
	<property name="LOG_HOME" value="/ztsoft/usr/wasadm/logs/pgw-pay-test" />
	<property name="ServerId" value="01" />
	
	<appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
		<encoder charset="UTF-8">
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger -%msg%n</pattern>
		</encoder>
	</appender>

	<appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<!-- 可让每天产生一个日志文件，最多30个，自动回滚 -->
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOG_HOME}/pgw-pay-test-${ServerId}-%d{yyyy-MM-dd-HH}.log</fileNamePattern>
		</rollingPolicy>
		<encoder>
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger -%msg%n</pattern>
		</encoder>
	</appender>

	<root level="INFO">
		<appender-ref ref="Console" />
		<appender-ref ref="RollingFile" />
	</root>
	
	<logger level="debug" name="com.i2f.ect.pgw.mapper" />
	
</configuration>